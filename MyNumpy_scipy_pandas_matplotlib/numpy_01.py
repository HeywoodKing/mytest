# -*- coding: utf-8 -*-


import numpy as np

# 创建数组
a = np.array([t**2 for t in range(8)])
print(a)

# 创建多维度数组
b = np.zeros([2, 3])
print(b)

# 线性等分函数
c = np.linspace(0, 4, 5)
print(c)

d = np.array(range(9))
print(d)

